/* RESET */
/* ==========================================================================
   Normalize.scss settings
   ========================================================================== */
/**
 * Includes legacy browser support IE6/7
 *
 * Set to false if you want to drop support for IE6 and IE7
 */
/* Base
   ========================================================================== */
/**
 * 1. Set default font family to sans-serif.
 * 2. Prevent iOS text size adjust after orientation change, without disabling
 *  user zoom.
 * 3. Corrects text resizing oddly in IE 6/7 when body `font-size` is set using
 *  `em` units.
 */
@import url(https://fonts.googleapis.com/css?family=Roboto:400,300,700,700italic,400italic,300italic);
/* line 23, ../sass/utils/_normalize.scss */
html {
  font-family: sans-serif;
  /* 1 */
  -ms-text-size-adjust: 100%;
  /* 2 */
  -webkit-text-size-adjust: 100%;
  /* 2 */
}

/**
 * Remove default margin.
 */
/* line 36, ../sass/utils/_normalize.scss */
body {
  margin: 0;
}

/* HTML5 display definitions
   ========================================================================== */
/**
 * Correct `block` display not defined for any HTML5 element in IE 8/9.
 * Correct `block` display not defined for `details` or `summary` in IE 10/11
 * and Firefox.
 * Correct `block` display not defined for `main` in IE 11.
 */
/* line 50, ../sass/utils/_normalize.scss */
article,
aside,
details,
figcaption,
figure,
footer,
header,
hgroup,
main,
menu,
nav,
section,
summary {
  display: block;
  box-sizing: border-box;
}

/**
 * 1. Correct `inline-block` display not defined in IE 6/7/8/9 and Firefox 3.
 * 2. Normalize vertical alignment of `progress` in Chrome, Firefox, and Opera.
 */
/* line 72, ../sass/utils/_normalize.scss */
audio,
canvas,
progress,
video {
  display: inline-block;
  /* 1 */
  vertical-align: baseline;
  /* 2 */
}

/**
 * Prevents modern browsers from displaying `audio` without controls.
 * Remove excess height in iOS 5 devices.
 */
/* line 89, ../sass/utils/_normalize.scss */
audio:not([controls]) {
  display: none;
  height: 0;
}

/**
 * Address `[hidden]` styling not present in IE 8/9/10.
 * Hide the `template` element in IE 8/9/11, Safari, and Firefox < 22.
 */
/* line 99, ../sass/utils/_normalize.scss */
[hidden],
template {
  display: none;
}

/* Links
   ========================================================================== */
/**
 * Remove the gray background color from active links in IE 10.
 */
/* line 111, ../sass/utils/_normalize.scss */
a {
  background-color: transparent;
}

/**
 * Improve readability when focused and also mouse hovered in all browsers.
 */
/* line 120, ../sass/utils/_normalize.scss */
a:active, a:hover {
  outline: 0;
}

/* Text-level semantics
   ========================================================================== */
/**
 * Address styling not present in IE 8/9/10/11, Safari, and Chrome.
 */
/* line 132, ../sass/utils/_normalize.scss */
abbr[title] {
  border-bottom: 1px dotted;
}

/**
 * Address style set to `bolder` in Firefox 4+, Safari, and Chrome.
 */
/* line 140, ../sass/utils/_normalize.scss */
b,
strong {
  font-weight: bold;
}

/**
 * Address styling not present in Safari and Chrome.
 */
/* line 155, ../sass/utils/_normalize.scss */
dfn {
  font-style: italic;
}

/**
 * Address variable `h1` font-size and margin within `section` and `article`
 * contexts in Firefox 4+, Safari, and Chrome.
 */
/* line 164, ../sass/utils/_normalize.scss */
h1 {
  font-size: 2em;
  margin: 0.67em 0;
}

/**
 * Addresses styling not present in IE 8/9.
 */
/* line 200, ../sass/utils/_normalize.scss */
mark {
  background: #ff0;
  color: #000;
}

/**
 * Address inconsistent and variable font size in all browsers.
 */
/* line 239, ../sass/utils/_normalize.scss */
small {
  font-size: 80%;
}

/**
 * Prevent `sub` and `sup` affecting `line-height` in all browsers.
 */
/* line 247, ../sass/utils/_normalize.scss */
sub,
sup {
  font-size: 75%;
  line-height: 0;
  position: relative;
  vertical-align: baseline;
}

/* line 255, ../sass/utils/_normalize.scss */
sup {
  top: -0.5em;
}

/* line 259, ../sass/utils/_normalize.scss */
sub {
  bottom: -0.25em;
}

/* line 300, ../sass/utils/_normalize.scss */
nav ul,
nav ol,
nav li {
  margin: 0;
  padding: 0;
  list-style-type: none;
  list-style-image: none;
}

/* Embedded content
   ========================================================================== */
/**
 * 1. Remove border when inside `a` element in IE 8/9/10.
 * 2. Improves image quality when scaled in IE 7.
 */
/* line 317, ../sass/utils/_normalize.scss */
img {
  border: 0;
}

/**
 * Correct overflow not hidden in IE 9/10/11.
 */
/* line 328, ../sass/utils/_normalize.scss */
svg:not(:root) {
  overflow: hidden;
}

/* Grouping content
   ========================================================================== */
/**
 * Address margin not present in IE 8/9 and Safari.
 */
/* line 339, ../sass/utils/_normalize.scss */
figure {
  margin: 1em 40px;
}

/**
 * Address differences between Firefox and other browsers.
 */
/* line 347, ../sass/utils/_normalize.scss */
hr {
  -moz-box-sizing: content-box;
  box-sizing: content-box;
  height: 0;
  border: none;
}

/**
 * Contain overflow in all browsers.
 */
/* line 358, ../sass/utils/_normalize.scss */
pre {
  overflow: auto;
}

/**
 * Address odd `em`-unit font size rendering in all browsers.
 * Correct font family set oddly in IE 6, Safari 4/5, and Chrome.
 */
/* line 367, ../sass/utils/_normalize.scss */
code,
kbd,
pre,
samp {
  font-family: monospace, monospace;
  font-size: 1em;
}

/* Forms
   ========================================================================== */
/**
 * Known limitation: by default, Chrome and Safari on OS X allow very limited
 * styling of `select`, unless a `border` property is set.
 */
/**
 * 1. Correct color not being inherited.
 *  Known issue: affects color of disabled elements.
 * 2. Correct font properties not being inherited.
 * 3. Address margins set differently in Firefox 4+, Safari, and Chrome.
 * 4. Improves appearance and consistency in all browsers.
 */
/* line 394, ../sass/utils/_normalize.scss */
button,
input,
optgroup,
select,
textarea {
  color: inherit;
  /* 1 */
  font: inherit;
  /* 2 */
  margin: 0;
  /* 3 */
}

/**
 * Address `overflow` set to `hidden` in IE 8/9/10/11.
 */
/* line 412, ../sass/utils/_normalize.scss */
button {
  overflow: visible;
}

/**
 * Address inconsistent `text-transform` inheritance for `button` and `select`.
 * All other form control elements do not inherit `text-transform` values.
 * Correct `button` style inheritance in Firefox, IE 8/9/10/11, and Opera.
 * Correct `select` style inheritance in Firefox.
 */
/* line 423, ../sass/utils/_normalize.scss */
button,
select {
  text-transform: none;
}

/**
 * 1. Avoid the WebKit bug in Android 4.0.* where (2) destroys native `audio`
 *  and `video` controls.
 * 2. Correct inability to style clickable `input` types in iOS.
 * 3. Improve usability and consistency of cursor style between image-type
 *  `input` and others.
 * 4. Removes inner spacing in IE 7 without affecting normal text inputs.
 *  Known issue: inner spacing remains in IE 6.
 */
/* line 438, ../sass/utils/_normalize.scss */
button,
html input[type="button"],
input[type="reset"],
input[type="submit"] {
  -webkit-appearance: button;
  /* 2 */
  cursor: pointer;
  /* 3 */
}

/**
 * Re-set default cursor for disabled elements.
 */
/* line 453, ../sass/utils/_normalize.scss */
button[disabled],
html input[disabled] {
  cursor: default;
}

/**
 * Remove inner padding and border in Firefox 4+.
 */
/* line 462, ../sass/utils/_normalize.scss */
button::-moz-focus-inner,
input::-moz-focus-inner {
  border: 0;
  padding: 0;
}

/**
 * Address Firefox 4+ setting `line-height` on `input` using `!important` in
 * the UA stylesheet.
 */
/* line 473, ../sass/utils/_normalize.scss */
input {
  line-height: normal;
}

/**
 * 1. Address box sizing set to `content-box` in IE 8/9/10.
 * 2. Remove excess padding in IE 8/9/10.
 *  Known issue: excess padding remains in IE 6.
 */
/* line 483, ../sass/utils/_normalize.scss */
input[type="checkbox"],
input[type="radio"] {
  box-sizing: border-box;
  /* 1 */
  padding: 0;
  /* 2 */
}

/**
 * Fix the cursor style for Chrome's increment/decrement buttons. For certain
 * `font-size` values of the `input`, it causes the cursor style of the
 * decrement button to change from `default` to `text`.
 */
/* line 499, ../sass/utils/_normalize.scss */
input[type="number"]::-webkit-inner-spin-button,
input[type="number"]::-webkit-outer-spin-button {
  height: auto;
}

/**
 * 1. Address `appearance` set to `searchfield` in Safari and Chrome.
 * 2. Address `box-sizing` set to `border-box` in Safari and Chrome
 *  (include `-moz` to future-proof).
 */
/* line 510, ../sass/utils/_normalize.scss */
input[type="search"] {
  -webkit-appearance: textfield;
  /* 1 */
  -moz-box-sizing: content-box;
  -webkit-box-sizing: content-box;
  /* 2 */
  box-sizing: content-box;
}

/**
 * Remove inner padding and search cancel button in Safari and Chrome on OS X.
 * Safari (but not Chrome) clips the cancel button when the search input has
 * padding (and `textfield` appearance).
 */
/* line 523, ../sass/utils/_normalize.scss */
input[type="search"]::-webkit-search-cancel-button,
input[type="search"]::-webkit-search-decoration {
  -webkit-appearance: none;
}

/**
 * Define consistent border, margin, and padding.
 */
/* line 532, ../sass/utils/_normalize.scss */
fieldset {
  border: 1px solid #c0c0c0;
  margin: 0 2px;
  padding: 0.35em 0.625em 0.75em;
}

/**
 * 1. Correct `color` not being inherited in IE 8/9/10/11.
 * 2. Remove padding so people aren't caught out if they zero out fieldsets.
 * 3. Corrects text not wrapping in Firefox 3.
 * 4. Corrects alignment displayed oddly in IE 6/7.
 */
/* line 545, ../sass/utils/_normalize.scss */
legend {
  border: 0;
  /* 1 */
  padding: 0;
  /* 2 */
}

/**
 * Remove default vertical scrollbar in IE 8/9/10/11.
 */
/* line 558, ../sass/utils/_normalize.scss */
textarea {
  overflow: auto;
}

/**
 * Don't inherit the `font-weight` (applied by a rule above).
 * NOTE: the default cannot safely be changed in Chrome and Safari on OS X.
 */
/* line 567, ../sass/utils/_normalize.scss */
optgroup {
  font-weight: bold;
}

/* Tables
   ========================================================================== */
/**
 * Remove most spacing between table cells.
 */
/* line 578, ../sass/utils/_normalize.scss */
table {
  border-collapse: collapse;
  border-spacing: 0;
}

/* line 583, ../sass/utils/_normalize.scss */
td,
th {
  padding: 0;
}

/* Don't hover on touch */
/*.touch {
  *:hover {
    background: initial;
    border: initial;
  }
}
*/
/* Kickoff */
/* line 9, ../sass/_typography.sass */
h1, h2, h3, h4, h5 {
  font-family: "Roboto", sans-serif;
  font-weight: 700;
}

/* line 13, ../sass/_typography.sass */
body {
  font-family: "Roboto", sans-serif;
  font-weight: 400;
}

/* Theme Specific Styles */
/* line 5, ../sass/_base.sass */
html {
  box-sizing: border-box;
}

/* line 8, ../sass/_base.sass */
*, *:before, *:after {
  box-sizing: inherit;
}

/* line 11, ../sass/_base.sass */
* {
  -webkit-font-smoothing: antialiased;
  -ms-interpolation-mode: bicubic;
}

/* line 15, ../sass/_base.sass */
body {
  color: black;
  letter-spacing: 0.5px;
}

/* line 19, ../sass/_base.sass */
main {
  max-width: 2200px;
  width: 100%;
  margin: 0 auto;
  margin-top: 100px;
}
@media(max-width:768px) {
  /* line 19, ../sass/_base.sass */
  main {
    margin-top: 50px;
  }
}
/* line 35, ../sass/_base.sass */
.section {
  position: relative;
}

/* line 38, ../sass/_base.sass */
.width-restrict {
  max-width: 1100px;
  width: 90%;
  margin: 0 auto;
}

/* line 43, ../sass/_base.sass */
.pad {
  padding: 60px 0;
}

/* line 46, ../sass/_base.sass */
.extra-pad {
  padding: 120px 0;
}

/* line 50, ../sass/_base.sass */
.errors {
  color: red;
  text-align: left;
}

/* line 60, ../sass/_base.sass */
p, li {
  color: #1a1a1a;
  font-size: 14px;
  line-height: 18px;
}

/* line 65, ../sass/_base.sass */
a {
  color: #b300b3;
  text-decoration: none;
}

/* line 73, ../sass/_base.sass */
::selection {
  background: #e600e6;
  color: white;
}

/* line 77, ../sass/_base.sass */
::-moz-selection {
  background: #e600e6;
  color: white;
}

/* line 1, ../sass/layout/_footer.sass */
footer {
  text-align: center;
  background-color: #1a1a1a;
  padding: 40px;
}
/* line 6, ../sass/layout/_footer.sass */
footer p.small {
  font-size: 11px;
  margin: 10px 0;
  color: #e6e6e6;
}
/* line 11, ../sass/layout/_footer.sass */
footer .footer-nav {
  margin: 30px auto;
}
/* line 14, ../sass/layout/_footer.sass */
footer .footer-nav a {
  display: inline-block;
  margin: 0 5px;
  padding: 5px 15px;
  background-color: #333333;
  color: #e6e6e6;
  text-transform: uppercase;
}
/* line 24, ../sass/layout/_footer.sass */
footer .social {
  margin-top: 15px;
}
/* line 27, ../sass/layout/_footer.sass */
footer .social a {
  display: inline-block;
  margin: 0 5px;
  font-size: 16px;
  transition: all 150ms ease-out;
}
/* line 33, ../sass/layout/_footer.sass */
footer .social a:hover {
  color: #e6e6e6;
}

/* line 1, ../sass/layout/_header.sass */
header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100px;
  background-color: white;
  border-bottom: #cccccc;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
  z-index: 10;
}
/* line 12, ../sass/layout/_header.sass */
header #logo {
  position: absolute;
  top: calc(50% - 15px);
  transform: translate(0, -50%);
  left: 30px;
}
/* line 18, ../sass/layout/_header.sass */
header #logo img {
  height: 40px;
  width: auto;
}
/* line 22, ../sass/layout/_header.sass */
header .customer {
  position: absolute;
  right: 30px;
}
/* line 26, ../sass/layout/_header.sass */
header .search {
  position: absolute;
  right: 60px;
}
/* line 30, ../sass/layout/_header.sass */
header .cart {
  position: absolute;
  right: 90px;
}
@media(max-width:768px) {
  /* line 1, ../sass/layout/_header.sass */
  header {
    height: 50px;
  }
  /* line 36, ../sass/layout/_header.sass */
  header #logo {
    top: 50%;
  }
}
/* line 1, ../sass/layout/nav/_grabber.sass */
.grabber {
  width: 36px;
  height: 36px;
  position: absolute;
  top: 6px;
  right: 20px;
  cursor: pointer;
  z-index: 102;
  display: none;
}
/* line 12, ../sass/layout/nav/_grabber.sass */
.grabber .bars {
  position: absolute;
  top: 10px;
  left: 8px;
}
/* line 17, ../sass/layout/nav/_grabber.sass */
.grabber .bar-1, .grabber .bar-2, .grabber .bar-3 {
  position: absolute;
  width: 18px;
  top: 0;
  height: 2px;
  background-color: #1a1a1a;
  transition: all 150ms ease-out;
}
/* line 26, ../sass/layout/nav/_grabber.sass */
.grabber .bar-2 {
  top: 5px;
}
/* line 29, ../sass/layout/nav/_grabber.sass */
.grabber .bar-3 {
  top: 10px;
}
/* line 33, ../sass/layout/nav/_grabber.sass */
.grabber.closable .bar-1, .grabber.closable .bar-2, .grabber.closable .bar-3 {
  background-color: white;
}
/* line 36, ../sass/layout/nav/_grabber.sass */
.grabber.closable .bar-2 {
  opacity: 0;
}
/* line 39, ../sass/layout/nav/_grabber.sass */
.grabber.closable .bar-1 {
  transform: rotate(45deg);
  top: 5px;
}
/* line 43, ../sass/layout/nav/_grabber.sass */
.grabber.closable .bar-3 {
  transform: rotate(-45deg);
  top: 5px;
}
@media(max-width:768px) {
  /* line 1, ../sass/layout/nav/_grabber.sass */
  .grabber {
    display: block;
  }
}
/* line 1, ../sass/layout/nav/_mobile-nav.sass */
.mobile-nav {
  display: none;
}
/* line 4, ../sass/layout/nav/_mobile-nav.sass */
.mobile-nav nav {
  position: fixed;
  top: -100%;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 100;
  background-color: #b300b3;
  padding-top: 50px;
  opacity: 0.97;
  transition: all 150ms ease-out;
}
/* line 16, ../sass/layout/nav/_mobile-nav.sass */
.mobile-nav nav .top-level {
  display: block;
  width: 100%;
  text-align: center;
}
/* line 21, ../sass/layout/nav/_mobile-nav.sass */
.mobile-nav nav .top-level:hover {
  background-color: #b300b3;
}
/* line 24, ../sass/layout/nav/_mobile-nav.sass */
.mobile-nav nav .top-level a {
  color: #e6e6e6;
  display: block;
}
@media(max-width:768px) {
  /* line 1, ../sass/layout/nav/_mobile-nav.sass */
  .mobile-nav {
    display: block;
  }
}
/* line 2, ../sass/layout/nav/_nav.sass */
.main-nav nav {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  text-align: center;
  background-color: #e6e6e6;
  transition: all 150ms ease-out;
  font-size: 0;
}
/* line 12, ../sass/layout/nav/_nav.sass */
.main-nav nav .top-level {
  display: inline-block;
  padding: 7px 15px;
  position: relative;
  transition: all 150ms ease-out;
  cursor: pointer;
}
/* line 19, ../sass/layout/nav/_nav.sass */
.main-nav nav .top-level a {
  color: #b300b3;
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
  display: inline-block;
  transition: all 150ms ease-out;
}
/* line 27, ../sass/layout/nav/_nav.sass */
.main-nav nav .top-level.active, .main-nav nav .top-level:hover {
  background-color: #cd00cd;
}
/* line 30, ../sass/layout/nav/_nav.sass */
.main-nav nav .top-level.active a, .main-nav nav .top-level:hover a {
  color: #e6e6e6;
}
/* line 34, ../sass/layout/nav/_nav.sass */
.main-nav nav .sub-nav {
  display: none;
  position: absolute;
}
@media(max-width:768px) {
  /* line 1, ../sass/layout/nav/_nav.sass */
  .main-nav {
    display: none;
  }
}
/* line 1, ../sass/shared/_buttons.sass */
.btn {
  outline: none;
  background: white;
  padding: 11px 18px;
  color: black;
  font-weight: 500;
  cursor: pointer;
  border-top-left-radius: 4px;
  border-top-right-radius: 4px;
  border-bottom-left-radius: 4px;
  border-bottom-right-radius: 4px;
  font-size: 0.9rem;
  border: none;
  text-align: center;
  display: inline-block;
  transition: all 0.3s ease;
}
/* line 15, ../sass/shared/_buttons.sass */
.btn.fixed-width {
  width: 180px;
  padding-right: 0;
  padding-left: 0;
}
/* line 20, ../sass/shared/_buttons.sass */
.btn.padded-button {
  width: auto;
}
/* line 23, ../sass/shared/_buttons.sass */
.btn.cta-button {
  background: #b300b3;
  color: white;
}
/* line 27, ../sass/shared/_buttons.sass */
.btn.cta-button:enabled:hover {
  background: #e600e6;
  color: white;
}
/* line 31, ../sass/shared/_buttons.sass */
.btn.secondary-button {
  background: cyan;
  color: white;
}
/* line 35, ../sass/shared/_buttons.sass */
.btn.secondary-button:hover {
  background: black;
}
/* line 38, ../sass/shared/_buttons.sass */
.btn.link-button {
  background: transparent;
  border-color: black;
  color: black;
}
/* line 43, ../sass/shared/_buttons.sass */
.btn.link-button:hover {
  border-color: #b300b3;
  color: #b300b3;
}
/* line 47, ../sass/shared/_buttons.sass */
.btn.link-button.light-button {
  border-color: white;
  color: white;
}
/* line 51, ../sass/shared/_buttons.sass */
.btn.link-button.light-button:hover {
  border-color: #b300b3;
}
/* line 54, ../sass/shared/_buttons.sass */
.btn:disabled {
  opacity: 0.6;
}
/* line 57, ../sass/shared/_buttons.sass */
.btn.good {
  border-color: #b300b3;
  color: #b300b3;
}
/* line 61, ../sass/shared/_buttons.sass */
.btn.bad {
  border-color: cyan;
  color: cyan;
}

/* line 1, ../sass/shared/_input.sass */
input[type=text], input[type=search], input[type=email], input[type=password], input[type=number], input[type=tel], textarea {
  display: block;
  padding: 10px 20px;
  border-top-left-radius: 2px;
  border-top-right-radius: 2px;
  border-bottom-left-radius: 2px;
  border-bottom-right-radius: 2px;
  border: 1px solid #1a001a;
  color: black;
  outline: none;
  background: transparent;
  transition: all 150ms ease-out;
  -webkit-appearance: none;
  -moz-appearance: none;
  -ms-appearance: none;
  -o-appearance: none;
  appearance: none;
  width: 100%;
  margin: 5px auto;
  font-family: "Roboto", sans-serif;
}
/* line 15, ../sass/shared/_input.sass */
input[type=text]:hover, input[type=search]:hover, input[type=email]:hover, input[type=password]:hover, input[type=number]:hover, input[type=tel]:hover, textarea:hover {
  border-color: black;
}
/* line 18, ../sass/shared/_input.sass */
input[type=text]:focus, input[type=search]:focus, input[type=email]:focus, input[type=password]:focus, input[type=number]:focus, input[type=tel]:focus, textarea:focus {
  color: black;
  border-color: #b300b3;
}

/* line 22, ../sass/shared/_input.sass */
textarea {
  min-height: 5em;
}

/* line 25, ../sass/shared/_input.sass */
.btn {
  display: inline-block;
  border-top-left-radius: 2px;
  border-top-right-radius: 2px;
  border-bottom-left-radius: 2px;
  border-bottom-right-radius: 2px;
  -webkit-appearance: none;
  -moz-appearance: none;
  -ms-appearance: none;
  -o-appearance: none;
  appearance: none;
  border: none;
  background-color: #b300b3;
  color: white;
  font-family: "Roboto", sans-serif;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-weight: 700;
  font-size: 13px;
  padding: 12px 25px 11px 25px;
  transition: all 150ms ease-out;
}
/* line 40, ../sass/shared/_input.sass */
.btn:hover {
  background-color: purple;
}

/* line 2, ../sass/shared/_rte.sass */
.rte img {
  max-width: 100%;
}
/* line 5, ../sass/shared/_rte.sass */
.rte iframe {
  max-width: 100%;
  height: 0;
}

/* line 1, ../sass/grids/_grid.sass */
.grid {
  position: relative;
  overflow: hidden;
}
/* line 5, ../sass/grids/_grid.sass */
.grid .grid-item {
  display: inline-block;
  float: left;
  margin: 10px;
  width: calc(33.333% - 20px);
  text-align: center;
}
/* line 13, ../sass/grids/_grid.sass */
.grid.grid-4 .grid-item {
  width: calc(25% - 20px);
}
/* line 17, ../sass/grids/_grid.sass */
.grid.grid-2 .grid-item {
  width: calc(50% - 20px);
}
@media(max-width:768px) {
  /* line 22, ../sass/grids/_grid.sass */
  .grid .grid-item {
    width: calc(50% - 20px);
  }
  /* line 25, ../sass/grids/_grid.sass */
  .grid.grid-4 .grid-item {
    width: calc(50% - 20px);
  }
}
@media(max-width:500px) {
  /* line 30, ../sass/grids/_grid.sass */
  .grid .grid-item {
    width: 100% !important;
    float: none;
    display: block;
    margin: 0;
  }
}
/* line 8, ../sass/grids/_item.sass */
.grid .grid-item .product-image img {
  width: 100%;
  height: auto;
}
/* line 12, ../sass/grids/_item.sass */
.grid .grid-item .product-information {
  margin-top: 15px;
}
/* line 15, ../sass/grids/_item.sass */
.grid .grid-item .product-information .title {
  display: block;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-size: 16px;
  color: #b300b3;
}
/* line 22, ../sass/grids/_item.sass */
.grid .grid-item .product-information .price {
  display: block;
  margin-top: 5px;
  font-size: 12px;
  color: #999;
}
